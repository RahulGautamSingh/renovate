Renovate supports Apache Ant builds anchored at `build.xml`.
The `ant` manager follows imported XML files and referenced `.properties` files to resolve Maven-style dependencies managed through Ant tasks.
It supports both `groupId` / `artifactId` / `version` declarations and `coords=` declarations.
